# Zepto Data & AI Platform — Capstone

One connected repository, three linked modules, one story: raw web data →
clean relational store (`/data_pipeline`); a full profile-and-predict
analytics pipeline (`/analytics`); a grounded GenAI support assistant
(`/support_assistant`).

*(This README is being filled in as each module is completed — setup, run
steps, and design-decision summaries for all three modules land here.)*
