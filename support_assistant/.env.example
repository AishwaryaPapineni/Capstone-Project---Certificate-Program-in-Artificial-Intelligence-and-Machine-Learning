# MOCK_LLM: leave unset or "1" for the graded, fully offline baseline (default).
# Set to "0" only to try the optional real-LLM extension.
MOCK_LLM=1

# Only needed if MOCK_LLM=0. Free tier, no credit card required: https://console.groq.com
GROQ_API_KEY=
